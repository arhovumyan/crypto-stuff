# 🎨 System Architecture Diagrams

## Phase 1: Listener & Recorder (Current Implementation)

```
┌─────────────────────────────────────────────────────────────────────┐
│                        YOUR 5 LEADER WALLETS                        │
│  BiiduLC...  5aLY85p...  2pDhRxL...  79P5UPY...  5TjCevD...       │
└────────────────────────────┬────────────────────────────────────────┘
                             │ (on-chain transactions)
                             ↓
┌─────────────────────────────────────────────────────────────────────┐
│                         SOLANA BLOCKCHAIN                            │
│                        (Mainnet Confirmed)                           │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      HELIUS RPC PROVIDER                             │
│  ┌─────────────────┐              ┌──────────────────┐             │
│  │  WebSocket API  │              │  HTTP RPC API    │             │
│  │  logsSubscribe  │              │  getParsedTx     │             │
│  └────────┬────────┘              └────────┬─────────┘             │
└───────────┼─────────────────────────────────┼───────────────────────┘
            │ (real-time)                     │ (on-demand)
            ↓                                 ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    COPY TRADER BOT (Node.js)                        │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  WEBSOCKET MANAGER                                          │   │
│  │  • Maintains persistent connection                          │   │
│  │  • Subscribes to wallet logs                                │   │
│  │  • Auto-reconnection with backoff                           │   │
│  │  • Re-subscribes after reconnect                            │   │
│  └──────────────────────┬──────────────────────────────────────┘   │
│                         │ (transaction signatures)                  │
│                         ↓                                            │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  LISTENER SERVICE (Orchestrator)                            │   │
│  │  • Routes messages                                           │   │
│  │  • Coordinates components                                    │   │
│  │  • Error handling                                            │   │
│  └──────────────────────┬──────────────────────────────────────┘   │
│                         │                                            │
│                         ↓                                            │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  REDIS IDEMPOTENCY CHECK                                    │   │
│  │  • Fast in-memory lookup                                     │   │
│  │  • Key: processed:{signature}                               │   │
│  │  • TTL: 7 days                                               │   │
│  └──────────────────────┬──────────────────────────────────────┘   │
│                         │ (if not processed)                        │
│                         ↓                                            │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  TRANSACTION PARSER                                         │   │
│  │  • Fetch full parsed transaction                            │   │
│  │  • Compute token balance deltas                             │   │
│  │  • preTokenBalances → postTokenBalances                     │   │
│  │  • Identify: tokenIn (decreased) / tokenOut (increased)     │   │
│  └──────────────────────┬──────────────────────────────────────┘   │
│                         │ (DetectedSwap object)                     │
│                         ↓                                            │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  SWAP CLASSIFIER                                            │   │
│  │  • Is this a simple swap? (1 in, 1 out)                     │   │
│  │  • Identify DEX program                                      │   │
│  │  • Filter noise (transfers, LP, etc)                        │   │
│  └──────────────────────┬──────────────────────────────────────┘   │
│                         │ (validated swap)                          │
│                         ↓                                            │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  TRADE RECORDER                                             │   │
│  │  • Save to PostgreSQL                                        │   │
│  │  • Update wallet stats                                       │   │
│  │  • Log detection                                             │   │
│  └──────────────────────┬──────────────────────────────────────┘   │
└────────────────────────┼────────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      POSTGRESQL DATABASE                             │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │ followed_wallets                                              │ │
│  │ • Your 5 leader wallet addresses                              │ │
│  │ • enabled, config, score                                      │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │ leader_trades                                                 │ │
│  │ • signature (UNIQUE - idempotency)                            │ │
│  │ • token_in_mint, token_out_mint                              │ │
│  │ • amount_in, amount_out                                       │ │
│  │ • dex_program, raw_transaction                               │ │
│  │ • detected_at timestamp                                       │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │ copy_attempts (Phase 3)                                       │ │
│  │ • Our trade execution records                                 │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │ positions (Phase 3)                                           │ │
│  │ • Current token holdings                                      │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │ risk_events (Phase 2+)                                        │ │
│  │ • Risk triggers and alerts                                    │ │
│  └──────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                           REDIS CACHE                                │
│  • Processed transactions (fast dedup)                              │
│  • Rate limiting (future)                                            │
│  • Token metadata cache (future)                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow: Transaction Detection

```
Leader makes a swap on Solana
         ↓
Blockchain confirms transaction
         ↓
Helius WebSocket sends notification
   ← logsNotification({ signature, ... })
         ↓
WebSocket Manager receives message
         ↓
Listener Service routes to handler
         ↓
Check Redis: "processed:{signature}"?
   ├─ YES → Skip (already processed)
   └─ NO  → Continue
         ↓
Transaction Parser fetches full tx
   ← getParsedTransaction(signature)
         ↓
Compute token balance deltas
   preTokenBalances: { SOL: 10.5, USDC: 0 }
   postTokenBalances: { SOL: 9.0, USDC: 150 }
   Δ = { SOL: -1.5, USDC: +150 }
         ↓
Classify as swap?
   ✅ tokenIn: SOL (1.5)
   ✅ tokenOut: USDC (150)
         ↓
Trade Recorder saves to database
   INSERT INTO leader_trades (...)
         ↓
Mark as processed in Redis
   SET processed:{signature} = "1" EX 604800
         ↓
Log detection
   🎯 TRADE DETECTED: SOL → USDC
```

---

## Key Architectural Decisions

### 1. Delta-Based Detection vs Instruction Parsing

**Traditional Approach (Fragile):**
```
Parse Jupiter instruction → extract swap details
❌ Breaks when DEX changes format
❌ Requires maintaining parsers for each DEX
❌ Complex routes fail
```

**Our Approach (Robust):**
```
Compare token balances before/after transaction
✅ Works with ANY DEX
✅ Works through aggregators
✅ Future-proof
✅ Handles complex routes
```

### 2. Dual Idempotency Layer

**Why Both Redis + PostgreSQL?**

```
Redis:
  • Fast check (< 1ms)
  • In-memory
  • Prevents redundant processing
  • 7-day TTL (cleanup)

PostgreSQL:
  • UNIQUE constraint on signature
  • Permanent record
  • Fallback if Redis miss
  • Source of truth
```

### 3. WebSocket with Auto-Reconnect

**Why Not Polling?**

```
WebSocket:
  • Real-time (< 1s latency)
  • Efficient (push model)
  • Lower cost (fewer API calls)
  
Polling:
  • 5-30s delay
  • Inefficient (pull model)
  • Higher cost
```

**Reconnection Strategy:**
```
Disconnect detected
    ↓
Wait 1s → attempt 1
    ↓ (fail)
Wait 2s → attempt 2
    ↓ (fail)
Wait 4s → attempt 3
    ↓ (success)
Re-subscribe to all wallets
```

---

## Performance Profile

### Latency Breakdown

```
Event                          Time      Cumulative
────────────────────────────────────────────────────
Blockchain confirmation        ~400ms    400ms
WebSocket notification         ~100ms    500ms
Redis idempotency check        ~1ms      501ms
RPC getParsedTransaction       ~500ms    1001ms
Delta calculation + classify   ~10ms     1011ms
Database INSERT                ~50ms     1061ms
────────────────────────────────────────────────────
Total: ~1.1 seconds after confirmation ✅
```

### Resource Usage

```
Component          Memory    CPU      Network
─────────────────────────────────────────────
Node.js process    ~80MB     2-5%     -
WebSocket          ~5MB      1%       1KB/s
PostgreSQL         ~50MB     5%       -
Redis              ~20MB     1%       -
─────────────────────────────────────────────
Total              ~155MB    ~10%     1KB/s

Peak (processing):  ~200MB    ~20%     50KB/burst
```

### Throughput

```
Metric                          Capacity
────────────────────────────────────────
Max wallets monitored           50+
Max swaps processed/second      ~20
Database inserts/second         ~50
Redis operations/second         1000+
```

---

## Security Model (Phase 1)

```
┌─────────────────────────────────────────┐
│        ATTACK SURFACE                   │
├─────────────────────────────────────────┤
│ Helius API Key                          │
│  ├─ In .env (not committed)             │
│  └─ Read-only access                    │
│                                          │
│ Database                                 │
│  ├─ Local only (not exposed)            │
│  └─ Docker network                       │
│                                          │
│ Redis                                    │
│  ├─ Local only                           │
│  └─ No authentication (local)            │
│                                          │
│ Private Keys                             │
│  └─ NONE REQUIRED ✅                     │
└─────────────────────────────────────────┘

Risk Level: VERY LOW ✅
  • No funds at risk
  • Read-only operations
  • Local infrastructure
  • No exposed ports
```

---

## Monitoring Points

```
Health Checks:
  ✓ WebSocket connected?
  ✓ Last message received < 60s ago?
  ✓ Database responsive?
  ✓ Redis responsive?
  ✓ Error rate < threshold?

Metrics to Track:
  • Transactions detected/hour
  • Swaps classified/hour
  • Database writes/hour
  • Error rate
  • WebSocket reconnects
  • Average latency

Alerts (Future):
  ⚠️ WebSocket down > 5 minutes
  ⚠️ Database error rate > 5%
  ⚠️ No trades detected > 24 hours
  ⚠️ Redis connection lost
```

---

## Scalability Path

### Current (Phase 1)
```
5 wallets → 1 bot instance
```

### Future Scaling Options

**Vertical:**
```
50 wallets → 1 bot instance
  (WebSocket supports many subscriptions)
```

**Horizontal:**
```
500 wallets → 10 bot instances
  • Partition wallets across instances
  • Shared PostgreSQL
  • Shared Redis (with sharding)
```

**Geographic:**
```
Multi-region RPC providers
  • Primary: Helius (US)
  • Failover: QuickNode (EU)
  • Lower latency
```

---

## Phase 2 Preview

```
                    Phase 1 (Current)
                           ↓
                    Detected Swap
                           ↓
        ┌──────────────────────────────────┐
        │      RISK ENGINE (Phase 2)       │
        │  • Check token safety            │
        │  • Verify liquidity              │
        │  • Position limits               │
        │  • Daily loss tracking           │
        └──────────────┬───────────────────┘
                       │ (if passes)
                       ↓
        ┌──────────────────────────────────┐
        │   JUPITER QUOTE (Phase 2)        │
        │  • Get expected output           │
        │  • Calculate slippage            │
        │  • Estimate fees                 │
        └──────────────┬───────────────────┘
                       │
                       ↓
        ┌──────────────────────────────────┐
        │   PAPER TRADER (Phase 2)         │
        │  • Simulate execution            │
        │  • Track virtual portfolio       │
        │  • Calculate PnL                 │
        └──────────────────────────────────┘
```

---

**See GETTING_STARTED.md to run this system! 🚀**
